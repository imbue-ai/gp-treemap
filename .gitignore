node_modules/
playwright-report/
test-results/
.DS_Store

# GrandPerspective source (GPL) — used only as a reference during development.
# Kept outside version control so this repo can ship under a permissive license.
GrandPerspective-3_6_4/

# Scratch / editor state
.claude/
*.swp
*.swo

# Scan-tool outputs that sometimes land in the working tree
gp-treemap-*.html
